package com.google.android.gallery3d.exif;

class h
{
  public static final boolean g(short paramShort)
  {
    return (paramShort >= -64) && (paramShort <= -49) && (paramShort != -60) && (paramShort != -56) && (paramShort != -52);
  }
}