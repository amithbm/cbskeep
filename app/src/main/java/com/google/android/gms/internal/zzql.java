package com.google.android.gms.internal;

import android.accounts.Account;
import android.util.Log;

public class zzql
{
  public static String zzd(Account paramAccount)
  {
    if (paramAccount == null)
      return "null";
    if (Log.isLoggable("GCoreUlr", 2))
      return name;
    return "account#" + name.hashCode() % 20 + "#";
  }

  public static String zzf(Integer paramInteger)
  {
    if (paramInteger == null)
      return "(null)";
    if (Log.isLoggable("GCoreUlr", 2))
      return String.valueOf(paramInteger);
    return "tag#" + paramInteger.intValue() % 20;
  }
}